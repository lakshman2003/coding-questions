#include <bits/stdc++.h>
using namespace std;

const int MOD = 1000000007;

int countDiceSequences(int N, vector<int> rollMax)
{
    vector <>
}

int main()
{
    int N;
    cin >> N;
    vector<int> rollMax(6);
    for (int i = 0; i < 6; i++)
        cin >> rollMax[i];
    cout << countDiceSequences(N, rollMax);
}